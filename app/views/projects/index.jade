extends ../layouts/default

block body_content
  article.project-listing.panel
    div.container
      a.btn.new-btn(href='/project/new') New Project
      table.edit-content
        thead
          tr
            th.title Title 
              i.icon-chevron-up
            th.date Updated Date
              i.icon-chevron-up
            th.by Updated By
              i.icon-chevron-up
            th.edit Edit
            
        tbody
          - each project in projects
            tr
              td
                strong 
                  a(href='/project/' + project.slug + '/edit')
                    = project.title
              td
                a(href='/project/' + project.slug + '/edit')
                  = dateUpdated(project)
              td
                a(href='/project/' + project.slug + '/edit')
                  = updatedBy(project)
              td
                a(href='/project/' + project.slug + '/edit') Edit

block body_scripts
  script(src='/scripts/vendor/tablesorter/jquery.tablesorter.min.js')
  script.
    jQuery(function($){
      $('.edit-content').tablesorter();
    });